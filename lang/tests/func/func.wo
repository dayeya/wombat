fn int fib(n: int)
    if(n < 0) {
        putchar('n');
        putchar('o');
        putchar('p');
        putchar('\n');
        return 0;
    }
    if n == 0 { return 0; }
    if n <= 2 {
        return 1;
    }
    return fib(n - 1) + fib(n - 2);
end

fn int main()
    putnum(fib(6));
end